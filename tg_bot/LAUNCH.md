# 🚀 Настройка и запуск Telegram-бота

## 📋 Обзор проекта

Этот проект представляет собой Telegram-бота для парсинга судебных дел с сайта kad.arbitr.ru. Бот написан на **Python 3.11+** с использованием фреймворка **Aiogram 3.x** для работы с Telegram API.

### 🏗️ Архитектура бота

```
tg_bot/
├── src/
│   ├── bot.py              # Основной файл бота
│   ├── config.py           # Настройки и конфигурация
│   ├── keyboards.py        # Inline клавиатуры
│   ├── parser_service.py   # Сервис парсинга
│   ├── queue_manager.py    # Управление очередью задач
│   ├── rate_limiter.py     # Ограничение скорости запросов
│   └── backend_client.py   # Клиент для backend API
├── run_bot.py              # Скрипт запуска
├── .env                    # Конфигурация (не коммитить)
├── .env.example            # Пример конфигурации
├── requirements.txt        # Зависимости Python
└── README.md               # Общая документация
```

## ⚙️ Настройка бота

### 1. Получение токена бота

1. **Найдите @BotFather в Telegram**
2. **Создайте нового бота командой `/newbot`**
3. **Следуйте инструкциям:**
   - Укажите имя бота
   - Укажите username (должен заканчиваться на `bot`)
4. **Скопируйте токен** (он выглядит как `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### 2. Настройка конфигурации

1. **Скопируйте файл конфигурации:**
   ```bash
   cp .env.example .env
   ```

2. **Заполните `.env` файл:**
   ```env
   # Токен вашего бота
   TELEGRAM_BOT_TOKEN=ВАШ_ТОКЕН_ЗДЕСЬ

   # ID администраторов (получите через @userinfobot)
   ADMIN_IDS=[123456789, 987654321]

   # Остальные настройки можно оставить по умолчанию
   ```

### 3. Установка зависимостей

#### Рекомендуемый способ:
```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте окружение
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

#### Альтернативный способ (Docker):
```bash
# Соберите образ
docker build -t tg-bot .

# Запустите контейнер
docker run --env-file .env tg-bot
```

## 🚀 Запуск бота

### Локальный запуск:
```bash
# Из директории tg_bot/
python run_bot.py
```

### Docker Compose:
```bash
docker-compose up --build -d
```

### Проверка работы:
1. Найдите вашего бота в Telegram по username (@parskad_bot)
2. Отправьте команду `/start`
3. Если бот отвечает - настройка успешна!

### Возможные проблемы при запуске:

#### 🌐 Сетевые ошибки (ServerDisconnectedError):
Бот может показывать сетевые ошибки при первом запуске из-за:
- Flood control (слишком частые запросы при тестировании)
- Нестабильное интернет-соединение
- Временные проблемы Telegram API

**Решение:** Бот автоматически переподключается с экспоненциальной задержкой (до 10 попыток).

#### ⚠️ Логи показывают "Polling stopped":
Это нормально - бот завершает работу при получении сигнала прерывания (Ctrl+C).

#### 🔍 Проверка логов:
```bash
# Уровни логирования
LOG_LEVEL=DEBUG  # Максимальная детализация
LOG_LEVEL=INFO   # Основная информация (рекомендуется)
LOG_LEVEL=WARNING # Только предупреждения и ошибки
```

## 📱 Как работают команды бота

### Структура обработчиков команд

В файле `src/bot.py` команды регистрируются с помощью Dispatcher:

```python
from aiogram import Dispatcher
from aiogram.filters import Command

dp = Dispatcher()

# Регистрация команд
dp.message.register(cmd_start, Command("start"))
dp.message.register(cmd_help, Command("help"))
dp.message.register(cmd_parse, Command("parse"))
# ... другие команды
```

### Создание новой команды

1. **Создайте асинхронную функцию-обработчик:**
   ```python
   async def cmd_my_command(message: Message):
       """Обработчик для команды /my_command"""
       await message.answer("Привет! Это моя новая команда!")
   ```

2. **Зарегистрируйте команду в Dispatcher:**
   ```python
   dp.message.register(cmd_my_command, Command("my_command"))
   ```

3. **Полный пример с параметрами:**
   ```python
   from aiogram.filters import CommandObject

   async def cmd_greet(message: Message, command: CommandObject):
       name = command.args  # Получаем аргументы команды
       if name:
           await message.answer(f"Привет, {name}!")
       else:
           await message.answer("Привет! Укажи свое имя: /greet ВашеИмя")
   ```

### Типы команд в боте

#### Основные команды:
- `/start` - Приветствие и главное меню
- `/help` - Справка по командам
- `/parse <номер>` - Запуск парсинга дела
- `/status` - Проверка статуса задач
- `/cancel` - Отмена текущей задачи

#### Админ-команды:
- `/diagnostics` - Диагностика системы
- `/logs` - Выгрузка логов
- `/reinit` - Перезапуск драйвера

## 💬 Отправка сообщений

### Основные методы отправки сообщений

#### 1. Простое текстовое сообщение:
```python
await message.answer("Привет! Это ответ на ваше сообщение")
```

#### 2. Сообщение с клавиатурой:
```python
from keyboards import main_menu_kb

await message.answer(
    "Выберите действие:",
    reply_markup=main_menu_kb()
)
```

#### 3. HTML форматирование:
```python
await message.answer(
    "<b>Жирный текст</b>\n<i>Курсив</i>\n<code>Моноширинный</code>",
    parse_mode="HTML"
)
```

#### 4. Markdown форматирование:
```python
await message.answer(
    "*Жирный текст*\n_Курсив_\n`Моноширинный`",
    parse_mode="Markdown"
)
```

### Продвинутые возможности

#### Отправка файлов:
```python
# Отправка документа
await message.reply_document(
    document=open("file.pdf", "rb"),
    caption="Описание файла"
)
```

#### Редактирование сообщений:
```python
# Отредактировать свое сообщение
await message.edit_text("Обновленный текст")
```

#### Удаление сообщений:
```python
# Удалить сообщение через 5 секунд
await asyncio.sleep(5)
await message.delete()
```

## 🎛️ Работа с клавиатурами

### Inline клавиатуры

В файле `src/keyboards.py` определены функции создания клавиатур:

```python
from aiogram.utils.keyboard import InlineKeyboardBuilder

def main_menu_kb():
    kb = InlineKeyboardBuilder()
    kb.button(text="Статус", callback_data="status")
    kb.button(text="История", callback_data="history")
    kb.button(text="Режим", callback_data="mode")
    kb.adjust(3)  # Расположить в 3 колонки
    return kb.as_markup()
```

### Обработка callback-запросов

```python
async def on_callback(call: CallbackQuery):
    data = call.data  # Получаем callback_data

    if data == "status":
        await cmd_status(call.message)
    elif data.startswith("cancel"):
        await cmd_cancel(call.message)

    await call.answer()  # Подтверждаем нажатие
```

## 🔄 Асинхронная обработка задач

### Queue Manager

Бот использует очередь задач для обработки парсинга:

```python
# Отправка задачи в очередь
await queue_manager.submit(
    user_id=user_id,
    case_number=args,
    on_update=lambda upd: on_update_message(message, upd)
)

# Проверка активных задач
if queue_manager.has_active(user_id):
    await message.answer("У вас уже есть активная задача")
```

### Rate Limiting

Ограничение количества запросов:

```python
from rate_limiter import global_counter

ok, reason = global_counter.try_consume(user_id, 1)
if not ok:
    await message.answer(reason)
    return
```

## 🛡️ Проверка прав доступа

```python
def is_admin(user_id: int) -> bool:
    return user_id in settings.ADMIN_IDS

async def admin_command(message: Message):
    if not is_admin(message.from_user.id):
        return  # Игнорируем команду

    # Код для админов
    await message.answer("Команда выполнена!")
```

## 📊 Логирование

### Настройка логирования:

```python
import logging
from loguru import logger

# Настройка уровня логирования
log_level = settings.LOG_LEVEL.upper()
logger.remove()
logger.add(lambda msg: print(msg, end=""), level=log_level)
```

### Логирование действий:
```python
logger.info(f"Пользователь {user_id} запустил парсинг дела {case_number}")
logger.error(f"Ошибка при обработке: {e}")
```

## 🔧 Расширение функциональности

### Добавление новой команды:

1. **Создайте функцию-обработчик в `bot.py`:**
   ```python
   async def cmd_weather(message: Message, command: CommandObject):
       city = command.args
       if not city:
           await message.answer("Укажите город: /weather Москва")
           return

       # Логика получения погоды
       weather = get_weather(city)
       await message.answer(f"Погода в {city}: {weather}")
   ```

2. **Зарегистрируйте команду:**
   ```python
   dp.message.register(cmd_weather, Command("weather"))
   ```

3. **Добавьте в справку `/help`:**
   ```python
   async def cmd_help(message: Message):
       await message.answer(
           "/weather <город> — погода\n" +
           # ... остальные команды
       )
   ```

### Добавление новой клавиатуры:

1. **Создайте функцию в `keyboards.py`:**
   ```python
   def weather_kb():
       kb = InlineKeyboardBuilder()
       kb.button(text="Москва", callback_data="weather_moscow")
       kb.button(text="СПб", callback_data="weather_spb")
       kb.adjust(2)
       return kb.as_markup()
   ```

2. **Используйте в обработчике:**
   ```python
   await message.answer(
       "Выберите город:",
       reply_markup=weather_kb()
   )
   ```

## 🐛 Обработка ошибок

### Try-except блоки:
```python
try:
    result = await some_async_function()
    await message.answer(f"Результат: {result}")
except Exception as e:
    logger.error(f"Ошибка: {e}")
    await message.answer("Произошла ошибка. Попробуйте позже.")
```

### Graceful shutdown:
```python
async def main():
    # Настройка бота...

    try:
        await dp.start_polling(bot)
    except KeyboardInterrupt:
        logger.info("Бот остановлен пользователем")
    except Exception as e:
        logger.error(f"Критическая ошибка: {e}")
    finally:
        await bot.close()
```

## 📈 Мониторинг и отладка

### Проверка здоровья:
```bash
# Проверка статуса бота
curl http://localhost:8080/health  # Если используется веб-интерфейс

# Проверка логов
tail -f logs/bot.log
```

### Debug режим:
```env
LOG_LEVEL=DEBUG
```

### Тестирование команд:
```bash
# Отправьте команды боту через Telegram
/start
/help
/parse A85-338/2023
/status
```

## 🔒 Безопасность

### Защита от злоупотреблений:
- **Rate limiting** - ограничение количества запросов
- **Admin-only команды** - проверка прав доступа
- **Input validation** - валидация входных данных

### Хранение чувствительных данных:
- Никогда не коммитьте `.env` файл
- Используйте сильные токены
- Ограничивайте права администраторов

## 🎯 Советы по разработке

1. **Используйте типизацию** - помогает избежать ошибок
2. **Разделяйте логику** - handlers, services, utils
3. **Тестируйте команды** - проверяйте на разных устройствах
4. **Мониторьте логи** - следите за ошибками и производительностью
5. **Делайте код читаемым** - используйте понятные имена функций

## 📚 Полезные ресурсы

- [Aiogram Documentation](https://docs.aiogram.dev/)
- [Telegram Bot API](https://core.telegram.org/bots/api)
- [Python Telegram Bot Examples](https://github.com/python-telegram-bot/python-telegram-bot/tree/master/examples)
- [Pydantic Settings](https://docs.pydantic.dev/latest/concepts/pydantic_settings/)

---

🎉 **Готово! Ваш Telegram-бот настроен и запущен!**

Если возникнут вопросы - обращайтесь к документации или проверяйте логи для диагностики проблем.
