# Telegram бот для парсинга kad.arbitr.ru (локальный парсинг)

## 📋 Системные требования

- **Python**: 3.11+ (рекомендуется 3.11 или 3.12 для максимальной совместимости)
- **ОС**: Windows 10/11, Linux, macOS
- **RAM**: Минимум 2GB
- **Диск**: Минимум 1GB свободного места

## 🚀 Быстрый старт (локально)

### 1. Подготовка окружения
```bash
# Убедитесь, что находитесь в корневой директории проекта
# (где находятся папки tg_bot, backend, js_parser)

# Перейдите в папку бота
cd tg_bot

# Скопируйте файл конфигурации
cp .env.example .env
# Отредактируйте .env файл с вашими настройками
```

### 2. Установка зависимостей

#### ✅ Рекомендуемый способ (протестировано)
```bash
# Очистите кэш pip (важно!)
python -m pip cache purge

# Создайте чистое виртуальное окружение
python -m venv venv_clean

# Запустите скрипт установки
# Windows:
.\install_clean.bat

# Linux/Mac:
# chmod +x install_clean.sh && ./install_clean.sh
```

#### 🔧 Ручная установка (если скрипт не работает)
```bash
# Создайте виртуальное окружение
python -m venv venv_clean

# Активируйте окружение
# Windows:
venv_clean\Scripts\activate

# Linux/Mac:
source venv_clean/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 3. Запуск бота
```bash
# Активируйте виртуальное окружение (если не активировано)
venv_clean\Scripts\activate  # Windows
# source venv_clean/bin/activate  # Linux/Mac

# Запустите бота (рекомендуется, автоматически настраивает пути)
python run_bot.py

# Или напрямую из папки src (требует ручной настройки PYTHONPATH)
cd src && PYTHONPATH=../../backend:../.. python bot.py
```

## 🐳 Docker (Альтернативный способ)

```bash
# Скопируйте конфигурацию
cp .env.example .env

# Соберите и запустите через Docker Compose
docker-compose up --build -d

# Или вручную:
docker build -t tg-bot .
docker run --name tg-bot --env-file .env --restart unless-stopped tg-bot
```

## ⚠️ Устранение проблем

### Ошибки установки зависимостей
Если возникают ошибки при установке:
1. **Проверьте версию Python**: используйте Python 3.11 или 3.12 вместо 3.13
2. **Очистите кэш**: `python -m pip cache purge`
3. **Создайте новое виртуальное окружение**: `python -m venv venv_clean`
4. **Смотрите подробный анализ**: файл `BUG.md`

### Ошибки запуска бота
- **"ModuleNotFoundError: No module named 'backend'"**
  - Убедитесь, что запускаете бота из папки `tg_bot`: `cd tg_bot && python run_bot.py`
  - Или используйте `python tg_bot/run_bot.py` из корневой директории проекта
  - Скрипт `run_bot.py` автоматически настраивает пути к модулям

- **"ImportError: attempted relative import with no known parent package"**
  - Используйте `python run_bot.py` вместо `python src/bot.py`
  - Или настройте PYTHONPATH: `cd src && PYTHONPATH=../../backend:../.. python bot.py`

- **"TelegramNetworkError: ServerDisconnectedError"**
  - Это нормальная ситуация при первом запуске
  - Бот автоматически переподключается (до 10 попыток)
  - Может быть вызвано flood control или сетевыми проблемами

- Убедитесь, что все переменные в `.env` заполнены корректно
- Проверьте токен бота в Telegram
- Убедитесь, что порты 80 и 443 доступны (для Selenium)

### Windows-specific проблемы
- Установите [Microsoft Visual C++ Build Tools](https://visualstudio.microsoft.com/visual-cpp-build-tools/)
- Включите "Виртуализацию" в BIOS для Docker
- Используйте PowerShell или cmd от имени администратора

## ⚙️ Конфигурация (.env)

Создайте файл `.env` на основе `.env.example`:

```env
# Токен Telegram бота (обязательно)
TELEGRAM_BOT_TOKEN=your_bot_token_here

# ID администраторов в формате JSON массива (обязательно)
ADMIN_IDS=[123456789,987654321]

# Настройки парсинга
GLOBAL_DAILY_LIMIT=10000  # Лимит задач в сутки
POLL_INTERVAL_SECONDS=1   # Интервал проверки статуса

# Прокси для Telegram (опционально)
TELEGRAM_PROXY_URL=socks5://proxy.example.com:1080

# BACKEND_BASE_URL - не используется в локальном режиме
```

## 🎯 Возможности бота

- ✅ **Локальный парсинг** kad.arbitr.ru через Selenium
- ✅ **Получение статуса** и истории дел
- ✅ **Ссылки на PDF** документы
- ✅ **Прогресс сообщений** в реальном времени
- ✅ **Админ-команды** для диагностики и управления
- ✅ **Ограничения**: до 10000 дел в сутки
- ✅ **Отмена задач** пользователем

## 📱 Команды бота

### Основные команды
- `/start`, `/help` — приветствие и справка
- `/parse <номер>` — запустить парсинг дела по номеру
- `/status` — текущий статус активных задач
- `/links <номер>` — получить ссылки по делу
- `/history` — история выполненных задач
- `/mode` — переключение режима (links/download)
- `/cancel` — отмена собственной активной задачи

### Админ команды
- `/diagnostics`, `/health` — диагностика системы
- `/logs` — выгрузка логов бота
- `/reinit` — перезапуск драйвера (недоступно в текущей версии)

## 🔧 Технические детали

### Структура проекта
```
project_root/
├── backend/           # Основной парсер и утилиты
│   ├── parser_simplified.py    # Класс KadArbitrParser
│   ├── selenium_config.py      # Конфигурация Selenium
│   └── ...
├── tg_bot/            # Telegram бот
│   ├── src/           # Исходный код бота
│   ├── requirements.txt        # Зависимости бота
│   ├── run_bot.py     # Скрипт запуска с настройкой путей
│   └── .env.example   # Пример конфигурации
└── js_parser/         # Альтернативный JavaScript парсер
```

### Архитектура
- **Бэкенд**: Aiogram 3.x (асинхронный Telegram фреймворк)
- **Парсинг**: Selenium WebDriver с undetected-chromedriver (использует `backend.parser_simplified.KadArbitrParser`)
- **База данных**: SQLite (через aiogram)
- **Кэширование**: Внутренняя память с TTL
- **Логирование**: Loguru с ротацией файлов
- **Очереди**: Асинхронный менеджер задач с поддержкой отмены

### Ограничения
- Режим `download` не отправляет файлы в Telegram (только ссылки)
- Максимум 1 активная задача на пользователя
- Глобальный лимит: 10000 задач в сутки
- Локальный парсинг без внешних API

## 📊 Мониторинг и диагностика

### Проверка здоровья
```bash
# В активированном виртуальном окружении
python -c "import sys; print(f'Python: {sys.version}'); import selenium; print('Selenium: OK')"
```

### Логи
Логи сохраняются в файле `logs/bot.log` с ротацией по дням.

### Производительность
- **CPU**: Минимум 2 ядра
- **RAM**: 2-4GB на задачу
- **Диск**: 1GB для логов и кэша
- **Сеть**: Стабильное интернет-соединение

## 🆘 Поддержка и решение проблем

### Частые проблемы

1. **"Module not found"**
   - Убедитесь, что активировано виртуальное окружение
   - Переустановите зависимости: `pip install -r requirements.txt`

2. **"Chrome driver not found"**
   - WebDriver Manager автоматически скачает нужную версию
   - Проверьте права на запись в папку проекта

3. **"Connection timeout"**
   - Проверьте интернет-соединение
   - Настройте прокси в `.env` если необходимо

4. **"Too many requests"**
   - Соблюдайте лимиты: максимум 10000 задач в сутки
   - Используйте прокси для распределения нагрузки

### Дополнительная информация
📄 Подробный анализ ошибок и решений: [`BUG.md`](BUG.md)

## 📝 Лицензия и условия использования

- Проект предназначен только для образовательных и исследовательских целей
- Соблюдайте законы и правила использования kad.arbitr.ru
- Не используйте для автоматизированного сбора данных в коммерческих целях

---

**🚀 Приятного использования!** Если возникнут вопросы, создайте Issue в репозитории.

